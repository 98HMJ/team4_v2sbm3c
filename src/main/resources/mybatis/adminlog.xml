<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.adminlog.AdminlogDAOInter">
  <!-- insert 데이터 삽입 -->
  <insert id="create" parameterType="dev.mvc.adminlog.AdminlogVO">
    INSERT INTO adminlog(adminlogno, ip, rdate, adminno)
    VALUES (adminlog_seq.nextval, #{ip}, sysdate, #{adminno})
  </insert>

  <!-- adminlog 데이터 전체 조회 -->
  <select id="list" resultType="dev.mvc.adminlog.AdminlogVO">
    SELECT adminlogno, ip, rdate, adminno
    FROM adminlog
    ORDER BY adminno ASC, rdate DESC
  </select>
          
  <!-- adminlogno로 관리자 로그 정보 조회 -->
  <select id="read" resultType="dev.mvc.adminlog.AdminlogVO" parameterType="int">
    SELECT adminlogno, ip, rdate, adminno
    FROM adminlog
    WHERE adminlogno = #{adminlogno}
  </select>  

  <!-- adminno로 관리자 로그 정보 조회 -->
  <select id="read" resultType="dev.mvc.adminlog.AdminlogVO" parameterType="int">
    SELECT adminlogno, ip, rdate, adminno
    FROM adminlog
    WHERE adminno = #{adminno}
    ORDER BY adminno ASC, rdate DESC
  </select>
</mapper> 